# Actividad 2.6: Exploración de JavaScript en Backend + Cheerio

## Objetivo
Desarrollar un servicio backend mínimo pero funcional con Node.js y Express que consuma y procese mediante técnicas de web scraping (usando Cheerio) un archivo HTML estático de catálogo legacy, exponiendo endpoints REST estructurados en formato JSON.

## Instalación y Ejecución
1. Instalar las dependencias del proyecto:
   ```bash
   npm install